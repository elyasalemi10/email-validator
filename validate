#!/bin/bash
# Quick email validation: ./validate user@example.com
cd "$(dirname "$0")"
if [ -f "venv/bin/python" ]; then
  exec venv/bin/python validator.py "$@"
else
  exec python3 validator.py "$@"
fi
